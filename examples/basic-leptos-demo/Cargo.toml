[package]
name = "basic-leptos-demo"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
leptos = { version = "0.8.6", features = ["csr"] }
console_error_panic_hook = "0.1"
wasm-bindgen = "0.2"

[lib]
crate-type = ["cdylib"]
