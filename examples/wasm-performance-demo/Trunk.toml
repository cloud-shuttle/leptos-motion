[build]
target = "index.html"
dist = "dist"
public_url = "/"
cargo = { args = ["--manifest-path", "Cargo.toml"] }

[serve]
address = "127.0.0.1"
port = 8081
mime_types = { ".wasm" = "application/wasm" }

[watch]
watch = ["src"]
